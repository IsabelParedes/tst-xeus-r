name: xeus-kernel
channels:
  - https://repo.prefix.dev/emscripten-forge-dev
  - conda-forge
dependencies:
  - xeus-r >= 0.7.0
  - r-base < 4.5.0
  - r-ggplot2
  - r-tidyselect
  - r-generics
  - r-cli >= 3.6.5
